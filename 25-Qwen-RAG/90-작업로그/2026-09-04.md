# 2026-09-04

## 결정
- Qwen-RAG 독립 프로젝트
- 교본 수준 문서
- Windows WSL2
- Linux native
- vLLM / Qdrant
- Qwen3 Embedding 0.6B
- Reranker 2차
- A6000 48GB 최초 16K

## 실제 다음 작업
서버에서 아래 결과를 기록:
```bash
nvidia-smi --query-gpu=name,memory.total,driver_version,compute_cap --format=csv
cat /etc/os-release
python3 --version
docker --version
```

Windows면 PowerShell:
```powershell
nvidia-smi
wsl --version
wsl -l -v
docker --version
```
