# 업데이트 / 롤백

## 전
```bash
vllm --version
python --version
pip freeze > before-freeze.txt
nvidia-smi > before-gpu.txt
```

## Docker
image tag/digest 기록.

## model
가능하면 revision/commit 기록.

## 업데이트 후
1. `/v1/models`
2. 한국어
3. `/search`
4. `/ask`
5. Gold 10개
6. VRAM/속도 비교

## 롤백
기존 venv를 지우지 말고 새 venv를 만든다.
운영 Docker는 `latest`만 쓰지 않고 검증된 tag/digest를 고정한다.
