---
title: 전체 아키텍처 v0.8 — Knowledge + Action OPA
doc_path: 20-설계/01-전체-아키텍처.md
doc_category: 설계
style_version: rulmera-v0.5
render_profiles: quartz, obsidian, ppt
last_updated: 2026-09-17
---
# 전체 아키텍처 v0.8 — Knowledge + Action OPA

> [!summary]
> 이 문서는 제품 구조, 데이터 흐름, 권한 구조, 동작 원리를 설명하는 설계 기준 문서다.

> [!info]
> 표기 기준: 전문 용어는 가능하면 `원어(알기 쉬운 설명)` 형식을 사용하고, 공통 기준은 [[20-설계/00-용어-스타일-가이드]]를 따른다.

## 설계 핵심
Rulmera OPA Enterprise는 **지식운영 Core(핵심 엔진)**, **Action Execution(실행 계층)**, **배포환경 Adapter(환경별 연결부)** 를 분리한다.

## 용어 짧게 보기
- **Source(원천 자료)**: 파일서버, NAS, Git 등 실제 문서가 생기는 곳
- **Ingestion(수집 파이프라인)**: 새 자료를 감지하고 추출·분류·승인 후보화하는 흐름
- **Knowledge Layer(지식 계층)**: 승인된 지식과 메타데이터를 저장하는 계층
- **Deployment Fabric(배포 운영 틀)**: Cloudflare, On-Prem, Customer Cloud 등 다른 운영환경을 같은 제품 구조로 감싸는 틀

```mermaid
%%{init: {"theme":"base","themeVariables":{"primaryColor":"#F8FAFC","primaryTextColor":"#1F2937","primaryBorderColor":"#64748B","lineColor":"#64748B","secondaryColor":"#EAF4FF","tertiaryColor":"#EAFBF2","clusterBkg":"#F8FAFC","clusterBorder":"#CBD5E1","fontSize":"15px"}} }%%
flowchart TB
  subgraph Sources[Sources 원천 자료]
    FS[File Server / NAS<br/>파일서버·NAS]
    GIT[Git Repository<br/>소스 저장소]
    EXT[SharePoint / DMS<br/>향후 외부 문서관리]
  end

  subgraph Ingestion[Continuous Ingestion 지속 지식 수집]
    WATCH[Source Watcher<br/>변경 감지기]
    EXTRACT[Extract / OCR<br/>텍스트·표·이미지 추출]
    CLASS[AI Classifier<br/>문서종류·프로젝트·보안·버전 후보]
    CAND[Knowledge / Project Candidate<br/>지식·프로젝트 후보]
    APPROVE[Approval Workflow<br/>승인 절차]
  end

  subgraph Client[Client 사용화면]
    WEB[Browser / PWA<br/>브라우저·웹앱]
    AND[Android App<br/>안드로이드 앱]
    VOICE[Voice / STT<br/>음성입력]
  end

  subgraph App[Rulmera Core 핵심 제품]
    API[Application API<br/>서비스 입구]
    AUTH[Identity Adapter<br/>사용자 신원 연결]
    OPA[OPA Policy Engine<br/>접근정책 엔진]
    ROUTE[Logical Route Resolver<br/>원문 링크 해석기]
    AUD[Audit Service<br/>감사기록 서비스]
    CONTRACT[Answer Contract Builder<br/>표준 응답 조립기]
  end

  subgraph Knowledge[Knowledge Layer 승인 지식 계층]
    PG[(PostgreSQL + pgvector<br/>정본 메타데이터 + 벡터검색)]
    OBJ[(Object Storage Adapter<br/>원문·첨부 저장소)]
    RR[Reranker<br/>근거 우선순위 재정렬]
  end

  subgraph AI[Private AI 사내 AI]
    LLM[Qwen-class LLM<br/>질문응답 모델]
    EMB[Embedding Model<br/>의미벡터 생성 모델]
  end

  subgraph Deploy[Deployment Fabric 배포 운영 틀]
    DCF[Cloudflare Profile]
    DOP[On-Prem Profile]
    DCC[Customer Cloud Profile]
  end

  FS --> WATCH
  GIT --> WATCH
  EXT --> WATCH
  WATCH --> EXTRACT --> CLASS --> CAND --> APPROVE
  APPROVE --> OBJ
  APPROVE --> EMB --> PG

  VOICE --> API
  WEB --> API
  AND --> API
  API --> AUTH --> OPA
  OPA -->|authorized filters 허용필터| PG
  PG --> RR --> LLM
  LLM --> CONTRACT --> API
  API --> ROUTE
  API --> AUD

  DCF -. hosts .-> WEB
  DOP -. hosts .-> WEB
  DCC -. hosts .-> WEB

  classDef source fill:#EAF4FF,stroke:#2F80ED,stroke-width:2px,color:#102A43;
  classDef ingest fill:#FFF6E5,stroke:#F2994A,stroke-width:2px,color:#5C3B00;
  classDef client fill:#F3F0FF,stroke:#6C5CE7,stroke-width:2px,color:#2D1E5F;
  classDef core fill:#EAFBF2,stroke:#27AE60,stroke-width:2px,color:#114B2E;
  classDef ai fill:#FDECEC,stroke:#EB5757,stroke-width:2px,color:#7A1F1F;
  classDef dep fill:#F5F5F5,stroke:#7F8C8D,stroke-width:2px,color:#2C3E50;
  class FS,GIT,EXT source;
  class WATCH,EXTRACT,CLASS,CAND,APPROVE ingest;
  class WEB,AND,VOICE client;
  class API,AUTH,OPA,ROUTE,AUD,CONTRACT,PG,OBJ,RR core;
  class LLM,EMB ai;
  class DCF,DOP,DCC dep;
```

## 요청 처리 순서
```mermaid
%%{init: {"theme":"base","themeVariables":{"primaryColor":"#F8FAFC","primaryTextColor":"#1F2937","primaryBorderColor":"#64748B","lineColor":"#64748B","secondaryColor":"#EAF4FF","tertiaryColor":"#EAFBF2","clusterBkg":"#F8FAFC","clusterBorder":"#CBD5E1","fontSize":"15px"}} }%%
sequenceDiagram
  participant U as User(사용자)
  participant UI as Rulmera UI(사용자 화면)
  participant O as OPA(권한판정)
  participant R as Retrieval(지식검색)
  participant Q as Qwen(AI 답변)
  participant L as Route Resolver(원문링크 해석)

  U->>UI: 텍스트/음성 질문
  UI->>O: identity(신원) + question metadata
  O-->>UI: authorized scope(허용범위) / deny(거부)
  UI->>R: scope + question
  R-->>Q: 승인된 근거 Context(문맥)
  Q-->>UI: Structured Answer Contract(표준 응답)
  UI->>L: citation resource_id(근거 자원 ID)
  L-->>UI: 현재 배포환경의 원문 URL
  UI-->>U: 답변 + 근거 + 원문 링크
```

## 기술선택 기준
- Frontend: React + TypeScript + Vite + PWA
- Android: Capacitor
- Backend: Python FastAPI
- Policy: Open Policy Agent (Rego)
- DB: PostgreSQL + pgvector
- Object: S3-compatible abstraction
- Model serving: 실제 A6000 벤치 후 선택
- Streaming: SSE 우선
- Container: Docker Compose PoC, 운영은 환경별 확장
- Edge/Web Adapter: Cloudflare Workers/Static Assets 또는 On-Prem Web Gateway
- `workerd`: Workers 호환이 필요할 때 **선택적** 사용

## 경계 원칙
### Core에 들어가는 것
- Identity(사용자 신원)
- OPA Contract(정책 연동 규격)
- Knowledge Metadata(지식 메타데이터)
- Approval State(승인 상태)
- Retrieval(검색)
- Citation(근거 표시)
- Answer Contract(표준 응답)
- Logical Route(논리 원문 링크)
- Audit(감사 기록)

### Adapter로 분리하는 것
- Cloudflare Workers binding
- R2 endpoint / credentials
- On-Prem reverse proxy
- Customer Cloud ingress / load balancer
- 플랫폼별 secret / config

> [!warning]
> `workerd`는 Cloudflare Workers와 같은 계열 런타임이지만, Cloudflare 전체 플랫폼을 복제하는 것은 아니다. 자체 서비스 시 보안 경계는 VM / container / network 정책과 함께 구성한다.


## Action Execution(실행형 OPA) 추가
기존 질의/RAG 경로와 별도로 Qwen이 업무를 계획하고 Worker에 실행시키는 경로를 둔다.

```mermaid
flowchart LR
  U[User] --> API[Application API]
  API --> Q[Qwen<br/>Plan / Tool Call]
  Q --> AO[Action OPA<br/>실행권한 판정]
  AO -->|ALLOW| WC[Worker Controller]
  AO -->|DENY| AUD[Audit DENY]
  WC --> JQ[(Job Queue)]
  JQ --> EW[Execution Worker]
  EW --> VV[Validation]
  VV --> RS[(Result Store)]
  RS --> Q
  GIT[Git<br/>Optional Adapter] -.-> WC
  EW -. optional commit/push .-> GIT
```

### 두 Worker를 혼동하지 않는다
- `ingestion-worker`: 회사 원천자료를 감지·분류·색인하는 **지식 수집 Worker**
- `execution-worker`: 사용자의 승인된 작업을 파일/코드/문서/빌드/배포로 수행하는 **실행 Worker**

상세 설계: [[10-Qwen-Worker-실행아키텍처]], [[11-Worker-Job-Contract]]
